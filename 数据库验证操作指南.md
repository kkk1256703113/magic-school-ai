# EduVisualizer 数据库验证操作指南

> 💡 **适用于**: 验证用户注册信息是否成功写入数据库  
> 📅 **更新时间**: 2025-08-28

---

## 🔧 服务器和数据库信息

### 服务器信息
- **IP地址**: 45.77.86.20
- **SSH用户**: root
- **SSH端口**: 22

### 数据库信息
- **数据库类型**: PostgreSQL
- **数据库名**: eduvisualizer_db
- **数据库用户**: eduvisualizer_user
- **数据库密码**: EduViz2025Secure
- **数据库端口**: 5432

---

## 📋 详细验证步骤

### 第1步：连接到服务器

打开命令行工具（Windows用PowerShell，Mac/Linux用Terminal），输入以下命令：

```bash
ssh root@45.77.86.20
```

**说明**: 这个命令会通过SSH协议连接到服务器，如果成功会看到服务器的命令提示符。

### 第2步：连接数据库

在服务器上执行以下命令连接到PostgreSQL数据库：

```bash
PGPASSWORD=EduViz2025Secure psql -U eduvisualizer_user -d eduvisualizer_db -h localhost
```

**命令解释**:
- `PGPASSWORD=EduViz2025Secure` - 设置数据库密码环境变量
- `psql` - PostgreSQL的命令行客户端
- `-U eduvisualizer_user` - 使用的数据库用户名
- `-d eduvisualizer_db` - 连接的数据库名
- `-h localhost` - 数据库服务器地址（本地）

**成功连接后会看到**:
```
eduvisualizer_db=#
```

### 第3步：查看所有注册用户

在数据库提示符下输入以下SQL命令：

```sql
SELECT id, email, username, plan_type, api_calls_today, created_at FROM users ORDER BY id;
```

**命令作用**: 查看所有用户的基本信息，按用户ID排序

**预期输出示例**:
```
 id |              email              |  username   | plan_type | api_calls_today |         created_at         
----+---------------------------------+-------------+-----------+-----------------+----------------------------
  1 | admin@eduvisualizer.com         | admin       | yearly    |               0 | 2025-08-27 02:30:38.810244
  2 | free@test.com                   | freeuser    | free      |               0 | 2025-08-27 02:30:38.810244
  3 | monthly@test.com                | monthlyuser | monthly   |               0 | 2025-08-27 02:30:38.810244
  4 | test@example.com                | testuser    | free      |               0 | 2025-08-28 02:00:29.096418
  6 | frontend-test@example.com       | frontenduser| free      |               0 | 2025-08-28 02:07:25.123456
```

### 第4步：查找特定用户（可选）

如果要查找特定邮箱的用户，使用以下命令：

```sql
SELECT * FROM users WHERE email = 'your-email@example.com';
```

**注意**: 将 `your-email@example.com` 替换为要查找的实际邮箱地址

### 第5步：统计用户数量

查看总共有多少注册用户：

```sql
SELECT COUNT(*) as total_users FROM users;
```

**预期输出**:
```
 total_users 
-------------
           5
```

### 第6步：查看最近注册的用户

查看最近注册的5个用户：

```sql
SELECT id, email, username, created_at FROM users ORDER BY created_at DESC LIMIT 5;
```

---

## 🚪 安全退出

### 退出数据库
在数据库提示符下输入：
```sql
\q
```

### 退出服务器
在服务器命令行下输入：
```bash
exit
```

---

## 🔍 一键验证命令（高级用户）

如果不想逐步操作，可以使用以下一键命令直接查看用户信息：

```bash
ssh root@45.77.86.20 'PGPASSWORD=EduViz2025Secure psql -U eduvisualizer_user -d eduvisualizer_db -h localhost -c "SELECT id, email, username, plan_type, created_at FROM users ORDER BY created_at DESC LIMIT 10;"'
```

**说明**: 这个命令会连接服务器，查询最近注册的10个用户，然后自动断开连接。

---

## ⚠️ 注意事项

1. **密码安全**: 本指南包含真实密码，请妥善保管，不要分享给无关人员
2. **数据安全**: 只进行查询操作，不要执行DELETE、UPDATE等修改命令
3. **连接超时**: 如果长时间不操作，连接可能会超时，重新连接即可

---

## 🆘 常见问题

### 问题1: SSH连接失败
**解决方案**: 检查网络连接，确认服务器IP地址正确

### 问题2: 数据库密码错误
**解决方案**: 确认密码是 `EduViz2025Secure`，注意大小写

### 问题3: 找不到用户数据
**解决方案**: 确认用户已经成功注册，检查邮箱地址是否正确

---

## 📊 验证成功的标志

如果看到以下情况，说明用户注册系统工作正常：

1. ✅ 能够成功连接数据库
2. ✅ 用户表中有新增的用户记录
3. ✅ 用户信息完整（邮箱、用户名、注册时间等）
4. ✅ 密码已加密存储（password_hash字段有值）

---

**🎉 恭喜！您已掌握了数据库验证的完整流程！**